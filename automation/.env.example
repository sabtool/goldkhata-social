# Copy to automation/.env for local runs (never commit .env).
# On GitHub only META_ACCESS_TOKEN is a secret; the ids live in automation/config.json.

META_ACCESS_TOKEN=
META_PAGE_ID=
META_IG_USER_ID=
ASSET_BASE_URL=https://raw.githubusercontent.com/sabtool/goldkhata-social/main

# YouTube, when reels exist:
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
YOUTUBE_REFRESH_TOKEN=
